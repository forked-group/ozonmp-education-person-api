+ Исправить parseArguments. Разбор должен быть параметров, как в строке шела.
  Должны правильно разбираться элементы типа `var="some text"` -> [`var=some text`].
  Сейчас: `var="some text"` -> [`var=` `some text`]. Чтобы получить нужный результат,
  нужно записать "var=come text", что не айс.
- Усложнить модель, добавить поля: FirstName, LastName, Age, etc...
- Привести в соответствие с моделью команды Create и Edit.

